@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome to Single Ticketing System</h1>

    <!-- Buttons Container -->
    <div class="mt-4">
        <div class="d-flex justify-content-center">
            <div class="btn-group" role="group">
                <a href="@Url.Action("Index", "Driver")" class="btn btn-primary m-2">Driver</a>
                <a href="@Url.Action("Login", "Account")" class="btn btn-secondary m-2">Enforcer</a>
                <a href="@Url.Action("Login", "Account")" class="btn btn-danger m-2">Admin</a>
            </div>
        </div>
    </div>
</div>
